# lokiplay

Command-line tools for initializing, validating, deploying, and checking the
status of Loki game projects.

```sh
npm install --global lokiplay
lokiplay init ./my-game
lokiplay validate ./my-game
```

Deployment and status commands require a configured Loki API endpoint and
project credentials.
