# @lokiplay/ui-web

Web component overlay for Loki room status, players, invitations, and chat.

```sh
npm install @lokiplay/ui-web
```

Import `registerLokiOverlay` and call it once before adding a
`<loki-overlay>` element to the page.
